DECLARE
  DID EMPLOYEES.EMPLOYEE_ID%TYPE := &DID;
  FN EMPLOYEES.FIRST_NAME%TYPE; -- SCALAR VARIABLE, IT CAN STORE SINGLE VALUE AT A TIME
BEGIN
  SELECT FIRST_NAME
  INTO FN
  FROM EMPLOYEES
  WHERE DEPARTMENT_ID = DID;
  DBMS_OUTPUT.PUT_LINE(FN);
EXCEPTION
/*  WHEN TOO_MANY_ROWS THEN
    DBMS_OUTPUT.PUT_LINE('THIS DEPARTMENT HAS MULTIPLE EMPLOYEES.');
  WHEN NO_DATA_FOUND THEN
    DBMS_OUTPUT.PUT_LINE('NO SUCH DEPARTMENT EXIST IN COMPANY.');  */
  WHEN OTHERS THEN
    DBMS_OUTPUT.PUT_LINE('SOME OTHER ERROR OCCURRED, REFER DOCUMENTATION.');  
END;
/
