CREATE OR REPLACE PROCEDURE FORMAT_PHONE 
(P_PHONE_NO IN OUT VARCHAR2)
IS
BEGIN
    P_PHONE_NO  := 	'(' || SUBSTR(P_PHONE_NO,1,3) || ')' 
					|| SUBSTR(P_PHONE_NO,4,3) || '-' 
					|| SUBSTR(P_PHONE_NO,7);  /* ACTUAL TO FORMAL */
END;
/

-- 0123456789  -- IN VALUE
-- (012)345-6789  - OUT VALUE