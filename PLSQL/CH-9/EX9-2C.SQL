DECLARE
	PERIMETER NUMBER;
	AREA NUMBER;
	CIRCUM NUMBER;
BEGIN
	PERIMETER := S2_PACKAGE.PERI_AREA(20,AREA);
	DBMS_OUTPUT.PUT_LINE('PERIMETER : ' || PERIMETER || ' AND AREA : '|| AREA);
	PERIMETER := S2_PACKAGE.PERI_AREA(5, 5,AREA);
	DBMS_OUTPUT.PUT_LINE('PERIMETER : ' || PERIMETER || ' AND AREA : '|| AREA);
	CIRCUM := S2_PACKAGE.CIRCUM_AREA(10,AREA);
	DBMS_OUTPUT.PUT_LINE('CIRCUMFERENCE : ' || CIRCUM || ' AND AREA : '|| AREA);
	CIRCUM := S2_PACKAGE.CIRCUM_AREA(100,AREA);
	DBMS_OUTPUT.PUT_LINE('CIRCUMFERENCE : ' || CIRCUM || ' AND AREA : '|| AREA);
END;
/