CREATE OR REPLACE PACKAGE INTEREST_CALC_PACKAGE
IS
	FUNCTION INTEREST_CALC(P_AMT NUMBER,ROI NUMBER,PERIOD NUMBER) RETURN NUMBER;
	FUNCTION INTEREST_CALC(P_AMT PLS_INTEGER,ROI PLS_INTEGER,PERIOD PLS_INTEGER) RETURN PLS_INTEGER;
END INTEREST_CALC_PACKAGE;
/

/*CREATE A PACKAGE WHICH WILL HOLD OVER_LOADED FUNCTIONS NAMED INTEREST_CALC.

ONE FUNCTION SHOULD CALCULATE SIMPLE INTEREST AND ANOTHER COMPOUND INTEREST.

IT SHOULD TAKE 3 ARGUMENTS FOR PRINCIPAL AMOUNT (P_AMT) , RATE OF INTEREST (ROI) AND PERIOD.

SIMPLE INTEREST FORMULA : P_AMT * (ROI/100) * PERIOD

COMPOUND INTEREST FORMULA : P_AMT * POWER(1 + (ROI/100), PERIOD) - P_AMT

IF PARAMETER TYPE IS PLS_INTEGER IT SHOULD CALCULATE SIMPLE INTEREST, OTHERWISE CALCULATE COMPOUND INTEREST.

WRITE SPECIFICATION , BODY AND CALLING PROGRAM FOR THE SAME.

CALL FUNCTIONS USING ALL 3 STYLES OF PARAMETER PASSING.
*/